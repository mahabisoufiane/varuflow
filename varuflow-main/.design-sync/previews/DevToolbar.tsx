import { DevToolbar } from 'varuflow-ui';

export function Default() {
  return (
    <div style={{ position: 'relative', minHeight: '80px', background: '#f8f8f8' }}>
      <DevToolbar />
    </div>
  );
}
